{
  "name": "personalsite",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "deploy": "gh-pages -d . -b gh-pages -s \"{index.html,assets/**}\""
  },
  "devDependencies": {
    "gh-pages": "^5.0.0"
  }
}
